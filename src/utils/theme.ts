import { createTheme } from "@nextui-org/react";

const dark = createTheme({
  type: 'dark',
  className: 'dark',
});

const light = createTheme({
  type: 'light',
  className: 'light',
});

export {
  dark,
  light,
}